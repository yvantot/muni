FINISHED
- Module System
- Session mode
- Works as tab/newtab/popup
- Hover or click to reveal answer
- Allow hints feature
- User settings
- AI Prompt for automated q/a input
- Q/A input syntax
- Custom rules when popup will appear
- Export/Import data

- Different type of questions support, images, links support
- Stats visualization

function* idGenerator() {
    let id = 0; // Get the max ID from data storage, means this only need to run once
    while (true) {
        yield id++;
    }
}
